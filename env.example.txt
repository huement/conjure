# ADVICE: For values with spaces, escape with double quotes.
#
# Example:
#
# MAIL_FROM_NAME="John Doe"
#

# Primary parameters

APP_ENV=local
APP_DEBUG=true
APP_KEY=xlhF31NeOlibJcoOW9tvZg7TkHcAZI3a
APP_VERSION=
APP_URL=http://magic.app/
# APP_DOMAIN= CONFIGURE FOR PRODUCTION

SYSLOG_APPNAME=local.wp_conjure

DB_HOST=localhost
DB_DATABASE=wordpress
DB_USERNAME=homestead
DB_PASSWORD=secret

SESSION_COOKIE_NAME=wp_conjure # IMPORTANT Use underscore or dash instead of dot

STORAGE_PATH=/home/vagrant/code/storage

ROOT_REPORT_MAIL=example@localhost

# Secondary parameters
MAIL_DRIVER=smtp
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=homestead
MAIL_PASSWORD=secret
MAIL_ENCRYPTION=null
